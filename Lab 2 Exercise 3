# ask for the side length of the square
#display the square

n = int(input("Enter the length of the square: "))

for i in range(n):  #Loop through each row
    for j in range(n):  # Iterate from 0 to i (exclusive)
        if i == 0 or i == n - 1 or j == 0 or j == n - 1:  # Check if it's a border
            print("x", end=" ")  # Print 'x' for borders
        else:
            print(" ", end=" ")  # Print space for hollow part
    print()  # Move to the next line after each row
